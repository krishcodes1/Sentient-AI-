# Development/test-only dependencies. Production runtime uses
# requirements.txt; install both for local development:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
# Async SQLite driver used by the test suite's in-memory database
# (tests/conftest.py creates engines with "sqlite+aiosqlite://").
aiosqlite>=0.20.0,<1.0.0
